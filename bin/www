#!/usr/bin/env node
var	port = process.env.PORT || 80,
	http = require('../mess')(port, '{{session-secret}}'),
	dbdriver = require('../utils/dbdriver')('{{localdb}}', function() {
		http.listen(port, function() {

		});
	});
