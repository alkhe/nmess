#!/usr/bin/env node
var	port = process.env.PORT || 80,
	http = require('../mess')(port),
	dbdriver = require('../utils/dbdriver')('mess-database-replace-token', function() {
		http.listen(port, function() {

		});
	});
