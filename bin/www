#!/usr/bin/env node

var config = require('../config'),
	port = process.env.PORT || config.port || 3000,
	http = require('../app')(port, config["session-secret"]),
	dbdriver = require('../utils/dbdriver')(config.localdb, function() {
		http.listen(port, function() {

		});
	});
