#!/usr/bin/env node
var debug = require('debug')('mess'),
	port = process.env.PORT || 80,
	http = require('../mess')(port);

var server = http.listen(port, function() {
  debug('initialized on port ' + port);
});
